services:
  postgres:
    image: postgres:17
    restart: always
    environment:
      POSTGRES_USER: zenadmin
      POSTGRES_PASSWORD: temp
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - "5433:5432"
volumes:
  postgres: {}
